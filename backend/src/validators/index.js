const {
  validateSignup,
  validateLogin,
  validatePasswordChange,
  handleValidationErrors,
} = require("./authValidators");

module.exports = {
  validateSignup,
  validateLogin,
  validatePasswordChange,
  handleValidationErrors,
};
